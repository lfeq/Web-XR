export type ShareOptions = {
    id: string;
    /** The directory to upload from. */
    assetsDirectory: string;
    /** The BASE URL of the storage server. */
    storageUrl?: string;
    /** The access token to use. */
    access_token?: string;
    /** Open the uploaded files in the browser. */
    open?: boolean;
};
/**
 * Uploads all .glb files in the given directory to the storage server.
 * @param assetsDirectory The directory to upload from.
 * @param storageUrl Optional: The BASE URL of the storage server.
 */
export declare function share(opts: ShareOptions): Promise<void>;
