import commandLineArgs from "command-line-args";
import { stopOtherViteServers } from "../actions/stop-other-vite-server.js";
import { error } from "../utils/log.js";
const optionDefinitions = [
    { name: 'directory', type: String, multiple: false, defaultOption: true }
];
const options = commandLineArgs(optionDefinitions);
if (!options.directory || options.directory.length === 0) {
    error("Please specify a directory");
    process.exit(1);
}
stopOtherViteServers(options.directory);
