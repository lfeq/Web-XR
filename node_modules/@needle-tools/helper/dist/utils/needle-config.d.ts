export declare type NeedleConfig = {
    /** Resolved full path to the build directory */
    buildDirectory?: string;
    /** Resolved full path to the assets directory */
    assetsDirectory?: string;
};
declare type Opts = {
    directory?: string;
};
export declare function tryReadNeedleConfig(args: Opts): NeedleConfig | null;
export {};
